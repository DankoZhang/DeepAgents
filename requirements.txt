# DeepAgents 方法论驱动多 Agent 平台依赖
# 与 pyproject.toml 保持一致；建议使用 Python >= 3.13
# 优先：uv sync / pip install -e .

deepagents>=0.6.12
langchain>=1.3.13
langchain-openai>=1.3.5
langchain-anthropic>=1.4.8
langchain-mcp-adapters>=0.3.0
langgraph-checkpoint-redis>=0.1.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
rich>=15.0.0

# FastAPI + PostgreSQL + Alembic
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
alembic>=1.18.5
psycopg[binary]>=3.2.0
cryptography>=44.0.0
