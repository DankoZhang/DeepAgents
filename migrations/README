# Alembic 迁移

Schema 变更走版本化迁移。**API 启动不会自动 migrate**；部署或本地建库时单独执行：

```bash
python -m deepagents_app.db.migrate
# 或
alembic upgrade head
```

| 库状态 | `upgrade head` 结果 |
|--------|---------------------|
| 空库（还没表） | 新建表 |
| 已是最新版本 | 不变 |
| 有未应用的新迁移 | 更新表 |

## 常用命令

```bash
# 改完 ORM 后：根据差异生成迁移（请先确认连的是目标库）
alembic revision --autogenerate -m "describe change"

# 升级到最新（部署步骤）
python -m deepagents_app.db.migrate

# 回滚一步
alembic downgrade -1

# 查看当前版本
alembic current
alembic history
```

注意：只改 `models.py`、不生成并提交迁移脚本时，migrate 也不会改表。
